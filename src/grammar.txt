STRING := "[^"\\\\]*(?:\\\\.[^"\\\\]*)*"
IDENTIFIER := [a-zA-Z][a-zA-Z0-9]*
DECIMAL := [0-9]*,[0-9]+
INTEGER := -?[0-9]+
NUMBER := DECIMAL|INTEGER
OPERAND := NUMBER|IDENTIFIER
MATH_OPERATOR := +|-|*|/
COMPARISON_OPERATOR := <=|<|>=|>|=|!=
BOOLEAN_OPERATOR := AND|OR|&&|\|\|
OPERATOR := MATH_OPERATOR|COMPARISON_OPERATOR|BOOLEAN_OPERATOR
OPEN_PAREN := (
CLOSE_PAREN := )
CONDITIONAL := EXP ? EXP : EXP
EXP := OPERAND|CONDITIONAL
EXP := EXP OPERATOR EXP
EXP := OPEN_PAREN EXP CLOSE_PAREN
